# macOS
.DS_Store

# Editors
.idea/
.vscode/

# Agent session output
Claude outputs/

# Node (for when the monorepo lands — ticket 01)
node_modules/
.turbo/
.next/
dist/
*.tsbuildinfo

# Generated by Next on every dev/build; it references .next/, so committing it
# would break typecheck on a fresh clone.
next-env.d.ts

# Local env — never commit these
.env
.env.*
!.env.example
