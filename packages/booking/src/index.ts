/**
 * `@salon/booking` — the `BookingProvider` port and the pure `availability` module.
 *
 * May depend on `@salon/core` only. The port lands in ticket 17, slot generation
 * in ticket 19, the reservation transaction in ticket 20.
 */
export {};
