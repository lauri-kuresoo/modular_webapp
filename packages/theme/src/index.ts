/**
 * `@salon/theme` — Theme token sets and presets.
 *
 * May depend on `@salon/core` only. The token contract (semantic OKLCH colours,
 * type pairing, radius, density, shadow level) and the `themeTokens` seam land
 * in ticket 02; this package exists now so the dependency edges are real.
 */
export {};
